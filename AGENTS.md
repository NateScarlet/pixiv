# pixiv go client

Pixiv go 客户端，使用 PIXIV 网页 API。模块路径 `github.com/NateScarlet/pixiv`。

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues in this repo. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles map to same-named labels; `wontfix` already existed. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` at the repo root plus `docs/adr/`. See `docs/agents/domain.md`.
