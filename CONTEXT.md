# CONTEXT

## Series 系列

按顺序连载的作品集合。作品属于某一系列时，详情接口的 seriesNavData
声明其所属系列；该数据缺失表示作品不属于任何系列。

## 系列内位置 Series Position

作品在所属系列中的次序（order）。相邻章节（prev/next）指系列中紧邻
该作品的前一部与后一部作品。

## 记录 Record

库从 pixiv API 取回数据的统一形态：已知字段可直接读取，未建模字段
保留在原始响应中可获取；记录在取回后不再变化。